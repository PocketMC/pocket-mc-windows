<ui:FluentWindow x:Class="PocketMC.Desktop.Infrastructure.ProgressDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Progress" SizeToContent="Height" Width="480" MinWidth="360" MaxWidth="520" MinHeight="0"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid MaxWidth="480">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- No close button allowed in progress dialog -->
        <ui:TitleBar Grid.Row="0" Title="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" ShowMaximize="False" ShowMinimize="False" ShowClose="False" />

        <Grid Grid.Row="1" Margin="24,8,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ui:SymbolIcon Grid.Row="0" Symbol="ArrowSync24" FontSize="48" Margin="0,0,0,16" Foreground="{DynamicResource TextFillColorPrimaryBrush}" HorizontalAlignment="Center"/>

            <TextBlock Grid.Row="1" x:Name="MessageText" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,8" HorizontalAlignment="Center" TextAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>

            <TextBlock Grid.Row="2" x:Name="DetailText" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,24" HorizontalAlignment="Center" TextAlignment="Center" Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>

            <ProgressBar Grid.Row="3" x:Name="DialogProgressBar" Minimum="0" Maximum="100" Value="0" Height="4" HorizontalAlignment="Stretch" Margin="0,0,0,16"/>

            <ui:Button Grid.Row="4" x:Name="BtnCancel" Content="Cancel" Click="BtnCancel_Click" HorizontalAlignment="Center" Appearance="Secondary" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
